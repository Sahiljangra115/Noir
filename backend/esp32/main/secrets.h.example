/*
 * secrets.h.example — copy to secrets.h and fill in.
 *
 *     cp main/secrets.h.example main/secrets.h
 *
 * secrets.h is gitignored. Never commit real credentials.
 */
#pragma once

#define WIFI_SSID_STR  "your-wifi-ssid"
#define WIFI_PASS_STR  "your-wifi-password"

/* IP of the laptop running backend/main.py (the TCP server). */
#define SERVER_IP_STR  "192.168.1.10"
#define SERVER_PORT_NUM 9999
